{
  "Servers": {
    "1": {
      "Name": "${DOMAIN_NAME}",
      "Group": "${DOMAIN_NAME}",
      "Host": "${POSTGRES_HOST}",
      "Port": ${POSTGRES_PORT},
      "MaintenanceDB": "postgres",
      "Username": "yolo",
      "PassFile": "${CONF_DIR}/pgpass",
      "SSLMode": "prefer"
    }
  }
}
